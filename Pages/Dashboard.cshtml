@page
@model LoginApp.Pages.DashboardModel
@{
    Layout = "_Layout";
}

<h2 class="text-center">Bem-vindo, @Model.UsuarioNome! 👋</h2>
<p class="text-center">Você está logado com sucesso no sistema.</p>

<div class="text-center mt-4">
    <a asp-page="/Logout" class="btn btn-outline-danger">Sair</a>
</div>